const Connection = require('./connection');
const FakeClient = require('./clients/FakeClient');
const RealClient = require('./clients/RealClient');
const { PacketIntegrity, hasPadding } = require('./connection/integrity');

module.exports = { Connection, FakeClient, RealClient, PacketIntegrity, hasPadding };
